# Astroloh Requirements
# 
# For modern Python projects, we recommend using pyproject.toml with optional dependencies:
#
# Full installation (Linux/Windows with compilation support):
# pip install -e ".[full,dev]"
#
# macOS installation (without C compilation):
# pip install -e ".[macos,dev]"
#
# Minimal installation:
# pip install -e ".[minimal]"
#
# Development installation:
# pip install -e ".[dev]"

# Core dependencies (automatically installed)
-e .

# Default astronomy libraries for backward compatibility
# Note: pyswisseph may have compilation issues on macOS
# Use "pip install -e .[macos]" for macOS-compatible alternatives
pyswisseph==2.10.3.2  # Primary astrology library (may fail on macOS)
skyfield==1.49         # Alternative astronomy library (reliable on all platforms)
astropy==6.0.0         # Professional astronomy library (backup option)

# Development dependencies for backward compatibility
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.5.0
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1
bandit==1.7.5
safety==2.3.5